#include <iostream>
#include <fstream>
#include <vector>
#include <iterator>
#include <string>
#include <algorithm>
#include <iostream>
#include <cstdlib>
#include "my_hash.h"
#include "Superhero.h"
#include "csv.h"    

int main(int argc, char** argv){

	//parse off page_id?
	//io::CSVReader<3> in("marvel-wikia-data.csv");
	//in.read_header(io::ignore_extra_column, "urlslug", "id", "alignment", "eye_color", "hair_color", "sex", "gsm", "alive", "appearances", "first_appearance", "year");
	//std::string ve
	//while loop and stuff from class, IDK if needed
	return 0;
}
